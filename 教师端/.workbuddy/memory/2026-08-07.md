# 2026-08-07 工作日志

## 教师端智能体全面优化（进行中）

### 架构诊断
- 链路：前端 Agent 模式 → Python 后端(FastAPI+LangGraph workflow.py) → 降级前端 JS 工作流 → ReAct 兜底。
- 后端：server.py 入口；app/api.py(REST+SSE)、router.py(业务路由)、task_queue.py(异步+SSE)、exporter.py(落盘)、config.py(模型加密存储)、db.py(SQLite)、memory.py、kb.py。
- 依赖环境：D:\Python\Python312（fastapi/langgraph/docx/pptx/openpyxl/matplotlib 全装）；系统 PATH 的 python3.13 无依赖。
- 数据库无任何有效模型 Key（日志中均为测试失效 Key sk-test/sk-bad → 401）。

### 已完成的代码修复（workflow.py / backend）
1. 质量门（QUALITY_STANDARDS，19 类产物）：字数下限、必备章节、占位符黑名单、深度检查（正则计数）、PPT 页数下限；validate_product + build_quality_feedback；不合格带反馈重写，最多 AGENT_MAX_REFINE(默认3) 轮，仍不合格 ok=False 拒绝落盘。Settings 新增 quality_min_score(75)/quality_gate(默认开)。
2. _generate_and_refine 全面接入质量门（pid 参数）；lesson_plan 分节并行生成后整体验收+定向重写；ppt/exercise/animation/learning_analysis/explainer 全部接入。
3. 意图解析失败兜底改为 local_intent_parse（规则解析，复用 Mock 意图逻辑），不再硬编码默认数学教案。
4. aggregate 只传产物摘要(前200字)防上下文爆炸；质量报告与失败清单写入 final.quality/final.failed。
5. KnowledgeBase 单例缓存 _get_kb_cached（原来每任务重复加载 kg_data.json）。
6. 导出层修复：资源包目录名加 uuid 后缀防同秒撞名；延迟建目录+空目录自动清理（杜绝空资源包）；质量检测报告.md 随包落盘；docx 支持真实表格/**粗体**/中文字体(宋体正文+黑体标题)；pptx 修 subtitle 重复 bug；docx/pptx/xlsx 落盘后大小校验。
7. backend/exporter.py 同步修改（不再预建空目录）；router.py collect_products 过滤质量不合格产物并携带评分，全部不达标时任务显式失败；task_queue.unsubscribe 清理冗余代码。
8. 新建 tests/quality_loop.py：六场景（教案/PPT/教研报告/学情分析/习题/办公总结）自动化检测——工作流实跑+质量门+落盘文件校验（大小/可打开/页数），输出 tests/quality_report.json。

### 验证结果（Mock 模式）
- Mock 全链路冒烟通过：docx/pptx 真实生成（15页/65KB），目录名带随机后缀。
- quality_loop 正确拦截全部 Mock 敷衍产物（0/6 通过，符合预期）。

### 待办
- 等用户提供 DashScope API Key → 写入后端模型库 → 跑真实模型 quality_loop → 循环修复至 6/6 通过 → 交付产物包。

---

## 第二轮：真实模型验证与深水区修复（18:50–19:40）

### 真实模型运行（DashScope qwen3.7-plus，Key 仅 env 注入未落库）
- 首轮 3/6 通过：教案 100 分（11069字）、学情分析 100、习题 100（7920字）。
- 修复 3 个真 bug：
  1. **PPT 提示词内嵌 JSON 示例的花括号被 str.format 当作占位符** → KeyError '"type"'，PPT 永远生成失败。新增 `_fmt_prompt`（正则只替换已知 key），task_decompose/aggregate/_generate_and_refine 全部切换。
  2. **_generate_and_refine 旧校验器成功消息"通过"被当作失败原因** → 所有带 validator 的产物（教研报告/工作总结/通知等 10 类）永远被误阻。修复后第二轮 2/2 通过。
  3. matplotlib 图表 CJK 方框 + RGBColor(0-255) 未归一化报错 → 字体回退链 + 0-1 归一化。
- 前端修复（index.html / ui-controller.js）：
  - **Chat 无响应**：新增 isPureChatMessage 纯对话门（未点名产物的消息直接走流式问答，不触发工作流）；sendBeikeFollowup 包 try/catch 错误可见。
  - **删除历史记录刷新页面**：deleteBeikeHistoryRecord 仅在删除当前查看记录时才重置页面。
- 文档渲染硬升级（workflow.py 11.6 节）：
  - LaTeX 公式 matplotlib mathtext 渲染为透明 PNG 嵌入 docx（行内）与 pptx（混排行整行渲染）；$$ 归一为 $。
  - _strip_md_inline 清理 **、`、链接、图片标记；docx 标题/表格/列表全清理。
  - PPT 硬指标 ≥1MB：每页渐变背景图 + 每内容页配图（bar/line/pie/函数图像/示意图轮换）+ DPI 阶梯重建（200→400）。
- 检测结果：单元自测 PPT 1.12MB/24图/无残留；docx 公式成图、表格真实。

### 关键结论（回答用户"调试还是生成不了"）
- 后端进程需重启才能加载新代码；应用内配置的模型 Key 失效（401）会回退 Mock 产生敷衍产物。正常使用需在应用设置里配置有效模型 Key。

### 待办
- 六场景最终全量验证（tests/quality_report_final.json）→ 交付 → 删 tests/_run_real.py（含临时 Key）。
